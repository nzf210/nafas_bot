services:
  redis:
    image: redis:7-alpine
    container_name: nafas_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # TradingAgents Python Service
  trading-agents:
    build:
      context: ./services/trading_engine
      dockerfile: Dockerfile
    container_name: nafas_trading_agents
    ports:
      - "8889:8889"
    environment:
      - LLM_BASE_URL=${LLM_BASE_URL:-https://api.openai.com/v1}
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o}
      - OPENAI_API_BASE=${LLM_BASE_URL:-https://api.openai.com/v1}
      - OPENAI_API_KEY=${LLM_API_KEY}
    networks:
      - default
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8889/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  # NAFAS Bot Go Backend
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nafas_api
    ports:
      - "127.0.0.1:8990:8988"
    environment:
      - APP_ENV=${APP_ENV:-development}
      - APP_PORT=8988
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - DB_HOST=shared-postgres-db
      - DB_PORT=5432
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_WEBHOOK_URL=${TELEGRAM_WEBHOOK_URL}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - LLM_PROVIDER_URL=${LLM_PROVIDER_URL}
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL}
      - LLM_BASE_URL=${LLM_BASE_URL}
      - TRADING_AGENTS_URL=${TRADING_AGENTS_URL}
      - DEFAULT_MAX_RISK_PER_TRADE=${DEFAULT_MAX_RISK_PER_TRADE}
      - DEFAULT_MAX_ALLOCATION_PER_TRADE=${DEFAULT_MAX_ALLOCATION_PER_TRADE}
      - DEFAULT_DAILY_LOSS_LIMIT=${DEFAULT_DAILY_LOSS_LIMIT}
      - MAX_PAIRS_PER_USER=${MAX_PAIRS_PER_USER}
      - SCANNER_CYCLE_INTERVAL=${SCANNER_CYCLE_INTERVAL}
      - MIN_CONFIDENCE_THRESHOLD=${MIN_CONFIDENCE_THRESHOLD}
      - MAX_RISK_LEVEL=${MAX_RISK_LEVEL}
      - MIN_VOLATILITY_PERCENT=${MIN_VOLATILITY_PERCENT}
      - MIN_VOLUME_24H=${MIN_VOLUME_24H}
    depends_on:
      redis:
        condition: service_healthy
      trading-agents:
        condition: service_healthy
    networks:
      - default
      - shared-db-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8988/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  redis_data:

networks:
  default:
    name: nafas_bot_v010_default
  shared-db-network:
    external: true
